# .zshenv
#
# Every zsh instance (interactive and non-interactive) runs this so its
# only real use is for setting env vars of non-interactive shells

# Set basic PATH
export PATH=/opt/homebrew/bin:/usr/local/bin:/opt/homebrew/sbin:/usr/local/sbin:/usr/bin:/usr/sbin:/bin:/sbin
